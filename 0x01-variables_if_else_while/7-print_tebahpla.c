#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - prints the lowercase alphabet in reverse, followed by a new line
 * Return: 0 (Success)
 */
int main(void)
{
	char ch = 'z';

	while (ch >= 'a')
	{
		putchar(c);
		c--;
	}

	putchar('\n');
	return (0);
}
